{
  "name": "etalase",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply etalase-db --remote",
    "db:seed": "wrangler d1 execute etalase-db --remote --file=seed.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.30.0"
  }
}
